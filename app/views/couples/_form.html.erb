<%= simple_form_for [@couple, @booking] do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
  <div class="form-inputs">
    <%= f.input :start_date %>
  </div>
  <div class="form-inputs">
    <%= f.input :end_date %>
  </div>
  <div class="form-inputs">
    <%= f.submit %>
  </div>

<% end %>
